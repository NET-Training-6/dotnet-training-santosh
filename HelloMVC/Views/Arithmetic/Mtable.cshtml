@model (int from,int to)


<h1>Multiplication Table</h1>
<div class="row">

@for(int j = Model.from; j <= Model.to; j++)
    {
        <div class="col-3">
            @for (int i = 1; i <= 10; i++)
            {
                    <p>@j x @i = @(j * i)</p>
            }
        </div>
}

</div>